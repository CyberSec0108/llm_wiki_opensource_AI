# Dependencies for the optional local web UI (tools/server.py) and the LLM pipeline.
#   pip install -r requirements.txt
# Not needed if you only use Obsidian + git + Claude Code skills.
#
# NOTE: ASCII only. pip reads this file with the OS default encoding
# (cp949 on Korean Windows), so non-ASCII comments break the install.
# Korean explanations live in README.md instead.

fastapi>=0.110
uvicorn>=0.27
python-multipart>=0.0.9   # file upload on the "add source" form
markdown>=3.5             # read-only rendering in the wiki tab

# LLM providers -- you only need the one matching LLM_PROVIDER in .env
openai>=1.30              # openrouter (OpenAI-compatible API)
anthropic>=0.40           # anthropic direct
