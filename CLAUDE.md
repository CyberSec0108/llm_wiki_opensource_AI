# CLAUDE.md

이 볼트는 **LLM이 읽고 유지하는 개인 위키**다.
사람이 읽기 좋은 것보다, AI가 정확히 인용할 수 있는 것을 우선한다.

## 3계층 구조

| 계층 | 폴더 | 성격 |
|---|---|---|
| 원본 | `raw/` | **불변.** 내가 넣은 그대로. 절대 수정·삭제하지 않는다 |
| 위키 | `wiki/` | **파생.** 네가 컴파일해 유지한다. 버리고 다시 만들 수 있다 |
| 결과물 | `Output/` | 위키를 근거로 만든 글·문서 |
| 맥락 | `context/` | 나에 대한 메타 정보 |

각 폴더의 상세 규칙은 그 폴더의 `CLAUDE.md`에 있다. 그 폴더에서 작업하기 전에 읽어라.

## 맥락 파일 — 필요할 때 읽어라

- 내 배경·가치관·목표가 답에 영향을 주는 작업 → `context/나의 핵심 맥락.md`
- 진행 중인 작업과 관련된 요청 → `context/지금 하는 일.md`
- 노트 형식(type·status·링크·파일명)을 정할 때 → `context/위키 규약.md`

## 3가지 연산

내가 아래 단어를 쓰면 해당 절차를 따른다.

**ingest** — `raw/`의 새 자료를 위키에 반영
→ **`/ingest` 스킬의 절차를 그대로 따른다.** 절차를 여기에 중복해 적지 않는다.
핵심만: 기존 페이지 갱신이 새 페이지 생성보다 우선하고, 사용자에게 묻기 전에 위키를 건드리지 않는다.

**query** — 위키를 근거로 답변
→ **`/query` 스킬의 절차를 따른다.**
핵심만: index로 후보를 좁히고, 근거를 `[[링크]]`로 밝히며, **위키에 없으면 "위키에 없음"이라고 명시**한다.

**lint** — 위키 건강검진
→ **`/lint` 스킬의 절차를 따른다.**
핵심만: **보고만 하고 고치지 않는다.** 수정은 내 승인 후에 한다.

## 금지

- `raw/` 안의 파일을 수정하거나 삭제하지 마라. 추가만 가능하다.
- 출처 URL·경로를 지어내지 마라. 모르면 비워라.
- 여러 파일을 한 번에 옮기거나 이름을 바꾸기 전에 확인을 받아라 — `[[링크]]`가 깨진다.
- `wiki/log.md`의 기존 줄을 고치거나 지우지 마라. 추가만 한다.

## 응답 규칙

- 한국어로 답한다.
- 결론 먼저, 근거 나중.
- **설명은 개념부터 시작해 상세히. 비교 가능한 예시를 함께 든다.** 결론만 던지지 마라.
- **단일 출처로 결론 내지 마라.** 복수 출처의 일치점·불일치점을 나눠 제시하고 판단은 내가 한다.
- 내 지식 경계(아는 것 / 모르는 것)는 `context/나의 핵심 맥락.md`의 "4. AI 작업 규칙"에 있다.
- 위키를 근거로 답할 때는 근거 페이지를 `[[링크]]`로 밝힌다.

## 이 파일 유지 규칙

- 80줄 상한. 넘으면 해당 폴더의 `CLAUDE.md`나 `context/`로 내보낸다.
- 모든 줄은 "이게 AI 행동을 바꾸는가?"를 통과해야 한다.
- 같은 지적이 두 번 반복되면, 규칙으로 바꿔 여기 추가할 것을 먼저 제안하라.
